# This .env stores stores env vars specific to the auth project.
# Anything prefixed with NEXT_PUBLIC_ is publically accessible to any client.
# Otherwise, vars here are only avaiable in server contexts.
